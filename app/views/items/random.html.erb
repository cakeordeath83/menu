<div class="col-md-offset-2 col-md-8">
  
  <div style="text-align:center; margin-top:50px;">
    <h1> Surprise! </h1>
  </div>

  
  
    <div style="text-align:center; margin:40px;">
      <h4>How about a <%= @item.name %> <%= @item.category.category.singularize %> from <%= @item.retailer.name %>?</h4>
    </div>
    
    
    
     
    <table class="table">
      <tbody>
        <div class="row retailer-row">
          <tr>
            <td class=" retailer-image vert-align"><%= link_to image_tag(@item.retailer.asset.url(:thumb)), retailer_items_path(@item.retailer) %></td>
            <td class=" vert-align"><h5><%= link_to @item.retailer.name, retailer_path(@item.retailer) %></h5>
              <%= @item.retailer.address %><% unless @item.retailer.address2.blank?%>, <%= @item.retailer.address2 %><% end %><% unless @item.retailer.address3.blank?%>, <%= @item.retailer.address3 %><% end %>, <%= @item.retailer.city %>, <%= @item.retailer.postcode %>
            </td>
            <td class=" vert-align"><%= link_to "See menu", retailer_items_path(@item.retailer), class: "btn btn-default"%></td>
          </tr>
        </div>
      </tbody>
    </table>
  
 
    <div class="col-md-offset-3 col-md-6" style="text-align:center; margin-top:50px;">
      <h5 >No good? Click again for another suggestion </h5><br>
      <%= link_to "Surprise me", items_random_path, class:"btn btn-default" %>
    </div>
  
</div>
  
  
  
 
  